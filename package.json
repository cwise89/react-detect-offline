{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "rimraf packages/react-detect-offline/dist/ && tsc",
    "build-watch": "tsc --watch",
    "lint": "eslint src/ demo/src",
    "prettier": "prettier src/** demo/src/** --write",
    "ci": "yarn run build"
  }
}
